
services:
  postgres_db:
    image: postgres:latest
    container_name: todo_postgres_dev
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      # Use a dedicated volume for development data
      - postgres_dev_data:/var/lib/postgresql/data
      - ./ai/seed:/docker-entrypoint-initdb.d
    # Expose the port to your host machine (localhost)
    ports:
      - "5432:5432"
    networks:
      - todo_infra_network
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5

  # turn:
  #   image: coturn/coturn
  #   container_name: todo_turn_dev
  #   volumes:
  #     - ./turnserver.conf:/etc/coturn/turnserver.conf
  #   command: ["-c", "/etc/coturn/turnserver.conf"]
  #   # Host networking is simplest for local dev
  #   network_mode: host
  #   restart: unless-stopped

volumes:
  postgres_dev_data:
networks:
  todo_infra_network:
    driver: bridge
